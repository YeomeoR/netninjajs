const person = 'shaun';
// functions

// declare the arrow function
const greet = () => 'hello';
// store the result of the function as a variable and invoke it by setting the variable equal to the function name and parentheses
let resultOne = greet();
// log the new variable to the console
console.log(resultOne);

//methods

// when we invoke a method, we invoke it using dot notation We can store that in a variable too and then log it to the console with the new variable name:

// let resultTwo = person.toUpperCase();
// console.log(resultTwo);

